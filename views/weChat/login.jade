div(ui-content-for="title")
    span 信息系统登录
.scrollable
    .scrollable-content.section
        div(class='alert {{notice.type}}',ng-show='notice.show')
            a.close(ng-click='showMsg()', data-dismiss='alert') &times;
            |{{notice.msg}}
        form(role='form', ng-submit='login(user)', name="myform")
            fieldset
                legend Login
                .form-group.has-error.has-feedback
                    label(name='account') Account
                    input.form-control(type='text',name='account', ng-model='user.account',placeholder="Enter account")
                .form-group
                    label(name='password') Password
                    input.form-control(type='password', name='password',ng-model='user.password',placeholder='Password')
            button.btn.btn-primary.btn-block(ng-disabled='(!myform.account.$dirty)&&myform.account.$valid') 登录
            div(ui-content-for='navbarAction')
                a.btn(ng-click='login(user)')
                    i.fa.fa-user
                    |登录






